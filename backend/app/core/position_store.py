""" In-memory store for currently OPEN positions (Paper and Live), keyed by a generated position_id. """ from __future__ import annotations from dataclasses import dataclass from datetime import datetime from typing import Optional from app.models.enums import Underlying, TradingMode, FyersOrderSide, OptionType from app.models.strategy_schemas import TradeSignal @dataclass class OpenPosition: position_id: str underlying: Underlying trading_mode: TradingMode signal: TradeSignal opened_at: datetime class PositionStore: def __init__(self): self._positions = {} def add(self, position): self._positions[position.position_id] = position def remove(self, position_id): popped = self._positions.pop(position_id, None) return popped def all(self): values = list(self._positions.values()) return values def for_underlying(self, underlying): matches = [] for p in self._positions.values(): if p.underlying == underlying: matches.append(p) return matches def count(self): n = len(self._positions) return n def compute_leg_payoff_at_spot(leg, spot): is_call = leg.option_type == OptionType.CE if is_call: intrinsic = max(spot - leg.strike, 0.0) else: intrinsic = max(leg.strike - spot, 0.0) if leg.side == FyersOrderSide.BUY: sign = 1 else: sign = -1 payoff = (intrinsic - leg.entry_price_hint) * leg.total_quantity * sign return payoff def compute_position_realized_pnl(signal, spot): total = 0.0 for leg in signal.legs: total = total + compute_leg_payoff_at_spot(leg, spot) return total def is_position_expired(signal, now): if not signal.legs: return False first_leg_expiry_date = signal.legs[0].expiry.date() today_date = now.date() if today_date >= first_leg_expiry_date: return True return False
